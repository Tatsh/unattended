--- linux-2.6.2-orig/init/do_mounts.c	2004-02-03 22:43:42.000000000 -0500
+++ linux-2.6.2/init/do_mounts.c	2004-02-16 20:03:42.000000000 -0500
@@ -396,7 +396,8 @@
 	if (is_floppy && rd_doload && rd_load_disk(0))
 		ROOT_DEV = Root_RAM0;
 
-	mount_root();
+        if((MAJOR(ROOT_DEV) != 0) || (MINOR(ROOT_DEV) != 0))
+          mount_root();
 out:
 	umount_devfs("/dev");
 	sys_mount(".", "/", NULL, MS_MOVE, NULL);
